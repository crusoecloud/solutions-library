#!/usr/bin/env bash
# Installed via `git config core.hooksPath .githooks` (see scripts/setup-hooks.sh).
set -euo pipefail

repo_root=$(git rev-parse --show-toplevel)
exec python3 "$repo_root/scripts/check_solutions_structure.py"
