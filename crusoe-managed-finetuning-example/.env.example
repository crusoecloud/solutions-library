# Crusoe Intelligence Foundry — fine-tuning API
# Copy to .env and fill in. .env is git-ignored; never commit real creds.
#   cp .env.example .env

# API key (bearer-style). Generate in the Crusoe Console under Security.
CRUSOE_API_KEY=your-api-key-here
